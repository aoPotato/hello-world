# SBTI 人格测试（镜像版）

该项目为静态单页应用，页面结构、题库、评分逻辑与视觉样式对齐 `https://sbti.unun.dev`。

## 本地运行

```bash
python3 -m http.server 8080
```

然后访问：`http://localhost:8080`

## 项目结构

- `index.html`：完整页面（样式 + 题目 + 计算逻辑）
- `image/`：结果页人格配图资源
